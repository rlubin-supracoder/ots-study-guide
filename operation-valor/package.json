{
  "name": "operation-valor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/assets.mjs && wrangler deploy --dry-run --outdir dist",
    "test": "node --test test/*.test.mjs",
    "deploy": "node scripts/assets.mjs && wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.2.12",
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
